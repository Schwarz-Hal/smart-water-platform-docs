---
id: user.data-file-explorer
title: 数据文件资源管理器
document_type: user_guide
document_version: 1.0.0
status: published
locale: zh-CN
audience: [platform_user, operator]
related_modules: [M02]
related_operators: []
related_apis: ["/api/v1/data-file-explorer", "/api/v1/data-collections", "/api/v1/data-files/uploads", "/api/v1/data-file-actions/move", "/api/v1/data-file-actions/copy", "/api/v1/data-file-actions/delete", "/api/v1/data-file-versions/{version_id}/preview", "/api/v1/data-files/{file_id}"]
owners: [product-team]
reviewed_at: 2026-08-29
summary: 通过文件资源管理器浏览数据文件、组织数据集、移动或深拷贝文件，并使用只读预览检查内容。
---

# 数据文件资源管理器

## 用途

【数据中心】中的【数据文件】页面用于像管理文件一样组织异构数据资源。根目录显示数据集和【无归属】入口；进入数据集后，可以查看其中的文件。文件的内容版本保持不变，移动、深拷贝或删除操作不会在页面上改写已有版本。

本页面与旧的 CSV 导入和数据资产页面并存。已经存在的 CSV 数据资产不需要为了使用资源管理器而强制迁移。

## 前置条件与角色

- 需要登录平台，并具有数据文件读取权限，才能浏览文件和打开只读预览。
- 需要数据文件写入权限，才能上传文件、移动文件或深拷贝文件；需要数据集写入权限，才能新建数据集。
- 需要数据文件删除权限，才能把文件移入回收站；删除数据集需要数据集删除权限。页面会根据当前账户权限显示可用操作。
- 普通用户只能看到自己拥有或被授权访问的资源；管理员可按管理范围查看资源。
- 平台内置示范文件可以用于查看和深拷贝，但不能移动或删除原文件。

## 操作步骤

### 1. 浏览根目录、无归属文件和数据集

1. 打开【数据中心】→【数据文件】。
2. 在左侧点击【根目录】，查看数据集入口和【无归属】入口。根目录本身不直接列出文件。
3. 点击【无归属】，查看尚未加入活动数据集的文件。
4. 点击左侧的数据集，或双击内容区中的数据集卡片，进入该数据集。
5. 使用顶部面包屑返回根目录或上一级位置；使用【刷新】重新读取当前目录。
6. 在【搜索当前目录】中输入名称的一部分，筛选当前目录中的条目。页面上的文件数、容量和选择数会随当前目录更新。

### 2. 新建数据集和上传文件

1. 点击【新建数据集】，填写名称；需要时填写说明，然后点击【创建】。
2. 进入目标数据集后，点击【上传文件】并选择本地文件。这样上传的文件会直接归入当前数据集。
3. 在【根目录】或【无归属】页面上传时，文件先作为无归属文件保存；之后可以把它移动到数据集。
4. 上传完成后，文件可能需要等待解析。解析完成前，文件仍可管理，但不能打开结构化预览。

预期结果：文件条目显示格式和大小，并可在预览中查看当前状态。上传不会覆盖原有版本；为同一逻辑文件再次上传会形成新的不可变版本。

### 3. 拖拽移动文件

1. 在当前目录单击文件进行选择；按住 Ctrl（macOS 使用 ⌘）或使用 Shift 可以选择多个文件。
2. 不按 Ctrl/⌘，将选中的文件拖到左侧某个数据集，或拖到【无归属】，完成移动。
3. 也可以将文件拖到当前内容区对应的目标位置。移动到数据集后，文件会从原位置消失并出现在目标数据集中。
4. 在数据集目录中打开文件右键菜单，选择【移动到…】，再选择目标数据集并确认。
5. 如果只想解除当前数据集的归属而保留文件，选择【移出数据集】。文件会回到【无归属】。

### 4. 使用 Ctrl/⌘ 拖拽或剪贴板深拷贝

1. 选择一个或多个文件。
2. 按住 Ctrl/⌘ 拖动文件到目标数据集，松开后创建文件副本；源文件保持不变。
3. 或按 Ctrl/⌘+C，页面提示已复制到内部剪贴板。
4. 进入目标数据集，在空白处右键选择【粘贴到此处】，或按 Ctrl/⌘+V。
5. 深拷贝是异步操作。提交后页面会提示操作已排队；稍后点击【刷新】确认副本出现在目标数据集中。

预期结果：副本拥有独立的文件资源和内容版本，原文件不会被修改。内置示范文件也可以深拷贝，但示范原文件仍保持只读。粘贴前应先进入目标数据集目录；根目录不能作为粘贴目标。

### 5. 使用右键菜单

在文件或数据集上点击右键，可以根据权限看到相应操作：

- 数据集：选择【打开数据集】进入目录；有权限时可选择【删除数据集】。
- 文件：选择【预览文件】打开只读预览；有权限时可选择复制、移动、移出数据集或删除。
- 当前目录空白处：可选择【新建数据集】、【上传文件】或【刷新】；有内部剪贴板内容时还可选择【粘贴到此处】。

右键菜单中的操作会作用于当前选择。若要只操作一个文件，先单击该文件；若要批量操作，先完成多选。

### 6. 打开只读预览

1. 在文件卡片上双击，或右键选择【预览文件】。
2. 查看格式、大小、版本、状态等元数据；当前版本摘要提供时也会显示 SHA256。
3. 查看结构化表格中的列名和前 50 行数据，可在表格区域滚动查看内容。
4. 点击右上角【×】，点击对话框外区域，或按 Esc 关闭预览。

预览只读显示文件内容，不提供字段映射、清洗或编辑控件。页面显示“仅显示前 50 行”时，表示文件内容没有被截断或修改。

### 7. 删除文件并处理回收站

1. 选中要删除的一个或多个文件。
2. 右键选择【删除所选】，确认提示中的文件数量。
3. 文件会移入回收站，而不是立即永久删除；原文件版本和历史引用不会因这一步被改写。
4. 如需恢复或永久清理，请进入回收站并使用账户拥有的恢复或清理权限。

删除数据集只归档数据集本身，不等同于删除其中的文件。若文件或版本仍被历史运行引用，永久清理可能需要等待相关依赖解除。

## 结果与失败处理

| 情况 | 页面表现 | 处理方式 |
| --- | --- | --- |
| 没有资源读取权限，或资源不存在 | 页面提示无法读取资源，接口不会透露其他用户资源详情 | 确认登录账户和授权范围；需要时联系管理员 |
| 文件正在解析 | 预览区域提示文件正在解析 | 等待解析任务完成后点击【刷新】再预览 |
| 文件格式不支持结构化预览 | 预览区域提示格式暂不支持 | 文件原件仍保留；使用支持的格式，或继续将其作为文件资源管理 |
| 文件解析失败 | 预览区域提示解析失败 | 原件仍保留；稍后重试或检查源文件后重新上传版本 |
| 移动目标不是活动数据集，或没有目标权限 | 移动操作失败 | 选择仍处于活动状态且账户有权访问的数据集 |
| 文件已归入其他数据集 | 添加或移动操作提示归属冲突 | 使用【移动到…】调整唯一归属，不要重复添加同一文件 |
| 深拷贝仍在排队 | 页面提示操作已排队，副本暂未出现 | 等待任务完成并点击【刷新】；暂时无法读取文件内容时稍后重试 |
| 尝试移动或删除内置示范文件 | 页面拒绝该操作 | 保留示范原文件；如需编辑或调整，请先深拷贝到自己的数据集 |
| 永久清理受到运行依赖阻塞 | 回收站保留该项并提示等待依赖 | 等相关运行结束或归档后，再按权限重试清理 |

## 快捷键

| 快捷键 | 作用 |
| --- | --- |
| Ctrl/⌘+单击 | 增加或取消选择一个文件 |
| Shift+单击 | 选择连续范围内的文件 |
| Ctrl/⌘+C | 将所选文件放入页面内部剪贴板 |
| Ctrl/⌘+V | 在当前数据集目录粘贴并深拷贝文件 |
| Ctrl/⌘+拖拽 | 将文件深拷贝到目标数据集 |
| Delete | 请求删除所选文件并移入回收站，需确认 |
| Esc | 关闭右键菜单或预览；没有打开的界面时清除选择 |

在搜索框或其他输入框中输入时，快捷键不会触发文件操作。
