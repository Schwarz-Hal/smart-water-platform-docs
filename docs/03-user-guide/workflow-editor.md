---
id: user.workflow-editor
title: 工作流编辑、发布、运行和结果查看
document_type: user_guide
document_version: 1.2.0
status: published
locale: zh-CN
audience: [platform_user]
related_modules: [M05, M07]
related_operators: []
related_apis: []
owners: [product-team]
reviewed_at: 2026-08-22
summary: 说明如何通过可视化工作区组装、发布、运行和复用分析流程。
---

# 工作流编辑、发布、运行和结果查看

## 用途

工作流将数据输入、数据处理、分析算子和结果输出连接为一个可复现的流程。草稿可以继续编辑；发布版本不可修改，运行时会冻结所使用的节点版本与参数。

## 打开工作区

从工作流列表新建时，先在“选择一个起始结构”中选择空白工作流或内置结构，再点击“创建草稿并进入编辑器”。编辑器使用 Dockview 工作区；右上角“窗口”菜单可显示或隐藏“算子目录”和“节点属性”面板，画布用于放置和连接节点。

## 编辑流程

1. 在“算子目录 / 可用节点”中按类别浏览数据源、变换、算法、控制、输出和组合节点。拖动节点到画布，或点击节点将其加入画布。
2. 选中节点，在“节点属性”中查看节点代码、版本、端口并填写参数。数据节点的“运行数据绑定”也在这里配置：数据集节点绑定数据集版本；数据通道节点按页面选择数据集、点位和指标。没有独立的运行绑定窗口。
3. 从一个节点的输出端口拖到另一个节点的兼容输入端口建立连线。类型不兼容、自己连接或形成环路的连线不会被接受；删除节点时同时检查受影响的连接。
4. 在节点属性中勾选需要暴露的输出端口，声明工作流最终输出。最终输出是运行结果中供工作流使用者查看或下游消费的端口，不会自动替代节点间连线。
5. 点击“创建草稿并进入编辑器”后，工作流已经是服务器草稿。后续修改会在短暂延迟后自动保存；需要立即保存或确认保存结果时，点击“保存草稿”。保存按草稿修订版本进行，发生并发冲突时先处理冲突再继续编辑。
6. 点击“校验图”。校验需要先保存草稿，并检查必要输入、端口连接、参数和输出声明；失败时按画布或结果提示修正。
7. 校验通过后点击“发布版本”。发布会生成不可修改的版本；后续修改应回到草稿并发布新版本，已发布版本的节点版本和参数保持不变。
8. 点击“运行已发布版本”，为数据节点提供所需绑定后启动运行。运行使用已发布版本，不直接运行未发布草稿。

## 创建和使用复合工作流节点

复合节点适合把一段已经验证、需要在多个工作流中重复使用的流程封装成一个目录节点。例如，一组固定的数据处理和质量检查步骤可以作为一个边界清晰的能力复用。若流程只在一个工作流中使用，直接在当前画布编辑通常更合适。

### 发布子工作流并注册版本

1. 先打开要封装的子工作流，在子工作流画布中完成连线、参数和输出设置。点击“保存草稿”；保存会自动触发校验，页面会显示校验结果。修正错误后再次保存，直到校验通过。
2. 点击“发布版本”，发布子工作流。注册复合算子时必须选择一个精确的已发布版本；不能用工作流名称或“最新版本”代替版本。已发布版本不可修改，后续调整要从它派生新的草稿并发布新版本。
3. 在该已发布版本的操作入口选择“注册为复合算子”，按向导填写节点编码、名称和说明。
4. 在向导中声明复合节点的边界输入、最终输出和对外参数：
   - 每个边界输入对应子工作流中一个没有上游连线、但会被流程内部使用的边界输出端口，并标明数据类型、是否必填以及用户可见名称。
   - 每个最终输出对应子工作流声明的最终输出端口；只有这些端口会作为复合节点的输出提供给外层工作流。
   - 每个对外参数映射到子工作流内的节点参数，可设置类型约束、默认值或是否必填。不要把仅用于画布布局的内容当作参数。
5. 在向导中确认所选工作流版本、端口和参数映射后提交注册。重复键、未知端口或参数、输入没有消费者、接口类型不一致等问题会阻止注册；按提示修正映射，必要时回到子工作流发布新版本后重新注册。

### 在外层工作流中使用

1. 回到外层工作流编辑器，在“算子目录 / 可用节点”的“组合”分类中找到已注册的复合节点，将它拖到画布，或点击节点加入画布。
2. 像普通节点一样连接输入和输出，并在“节点属性”中填写对外参数。复合节点引用的是注册时的精确已发布子工作流版本，不会随子工作流后来发布的新版本自动改变。
3. 双击复合节点可打开一个可关闭的只读嵌套画布标签，查看该精确版本的节点、连线、端口和注册接口。嵌套标签可以关闭；根工作流画布是固定入口，不能关闭。只读嵌套画布中的内容不能直接编辑，需回到子工作流草稿修改并发布新版本。
4. 修改外层工作流后点击“保存草稿”。保存会自动校验当前图；复合接口、参数、必需输入和输出声明不满足约束时，按错误路径修正后再保存。校验通过后点击“发布版本”。发布会冻结外层工作流及其复合依赖，之后草稿修改不会改变已发布版本。
5. 点击“运行”时，页面使用该工作流最后一个已发布版本。如果当前存在尚未发布的脏草稿，页面会先提示确认；确认运行仍使用最后一个已发布版本，取消则返回编辑。运行中的复合节点会显示为层级汇总，实际执行的是其冻结版本中的子节点。

### 版本与复现提示

复合节点注册的是子工作流的精确已发布版本；外层工作流发布时还会保存展开后的执行计划和依赖摘要。因此，子工作流或外层工作流后续产生新草稿、新发布版本，都不会回写历史版本或已经创建的运行。要使用改动后的子流程，先发布新版本，再按向导注册新的复合算子版本，并在外层工作流中替换节点后重新发布。

## 查看结果与失败处理

运行详情展示总体状态、进度、每个节点的状态与日志、节点输出和工作流最终输出。失败时先定位失败节点及其日志中的输入、参数或数据错误；修正草稿后重新保存、校验和发布新版本。绑定缺失、输入为空、端口或参数校验失败会阻止运行或使相应节点失败，不会自动补数据或自动重试。

## 参考资料

本页面说明当前平台工作流编辑器的确定性编辑、校验、版本和运行规则，无单独外部论文基准。
