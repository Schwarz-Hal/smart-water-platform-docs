---
id: user.workflow-editor
title: 工作流编辑、发布、运行和结果查看
document_type: user_guide
document_version: 1.1.0
status: published
locale: zh-CN
audience: [platform_user]
related_modules: [M05, M07]
related_operators: []
related_apis: []
owners: [product-team]
reviewed_at: 2026-08-21
summary: 说明如何通过可视化工作区组装、发布、运行和查看分析流程。
---

# 工作流编辑、发布、运行和结果查看

## 用途

工作流将数据输入、数据处理、分析算子和结果输出连接为一个可复现的流程。草稿可以继续编辑；发布版本不可修改，运行时会冻结所使用的节点版本与参数。

## 打开工作区

从工作流列表新建时，先在“选择一个起始结构”中选择空白工作流或内置结构，再点击“创建草稿并进入编辑器”。编辑器使用 Dockview 工作区；右上角“窗口”菜单可显示或隐藏“算子目录”和“节点属性”面板，画布用于放置和连接节点。

## 编辑流程

1. 在“算子目录 / 可用节点”中按类别浏览数据源、变换、算法、控制、输出和组合节点。拖动节点到画布，或点击节点将其加入画布。
2. 选中节点，在“节点属性”中查看节点代码、版本、端口并填写参数。数据节点的“运行数据绑定”也在这里配置：数据集节点绑定数据集版本；数据通道节点按页面选择数据集、点位和指标。没有独立的运行绑定窗口。
3. 从一个节点的输出端口拖到另一个节点的兼容输入端口建立连线。类型不兼容、自己连接或形成环路的连线不会被接受；删除节点时同时检查受影响的连接。
4. 在节点属性中勾选需要暴露的输出端口，声明工作流最终输出。最终输出是运行结果中供工作流使用者查看或下游消费的端口，不会自动替代节点间连线。
5. 点击“创建草稿并进入编辑器”后，工作流已经是服务器草稿。后续修改会在短暂延迟后自动保存；需要立即保存或确认保存结果时，点击“保存草稿”。保存按草稿修订版本进行，发生并发冲突时先处理冲突再继续编辑。
6. 点击“校验图”。校验需要先保存草稿，并检查必要输入、端口连接、参数和输出声明；失败时按画布或结果提示修正。
7. 校验通过后点击“发布版本”。发布会生成不可修改的版本；后续修改应回到草稿并发布新版本，已发布版本的节点版本和参数保持不变。
8. 点击“运行已发布版本”，为数据节点提供所需绑定后启动运行。运行使用已发布版本，不直接运行未发布草稿。

## 查看结果与失败处理

运行详情展示总体状态、进度、每个节点的状态与日志、节点输出和工作流最终输出。失败时先定位失败节点及其日志中的输入、参数或数据错误；修正草稿后重新保存、校验和发布新版本。绑定缺失、输入为空、端口或参数校验失败会阻止运行或使相应节点失败，不会自动补数据或自动重试。

## 参考资料

本页面说明当前平台工作流编辑器的确定性编辑、校验、版本和运行规则，无单独外部论文基准。
