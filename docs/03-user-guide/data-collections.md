---
id: user.data-collections
title: 异构数据集、文件版本与数据视图
document_type: user_guide
document_version: 0.1.0
status: draft
locale: zh-CN
audience: [platform_user, operator]
related_modules: [M02, M05]
related_operators: [data_file_input_v1]
related_apis: ["/api/v1/data-collections", "/api/v1/data-files/uploads", "/api/v1/data-file-versions/{version_id}/preview", "/api/v1/data-file-versions/{version_id}/views", "/api/v1/data-views/{view_id}"]
owners: [product-team]
reviewed_at: 2026-08-27
summary: 将格式不同的水务资料组织到数据集集合，上传文件、查看有限预览、创建不可变数据视图，并在工作流中绑定表格或时序输出。
---

# 异构数据集、文件版本与数据视图

## 用途

【数据中心】中的【数据集管理】用于把同一业务范围内的管网拓扑、历史流量或压力、传感器点位及其他资料放在一个数据集集合中。集合内的文件不要求使用相同格式或表头，适合先集中管理原始资料，再为工作流建立明确的读取视图。

这条链路不会取代旧的【数据源与导入】功能。原有的 CSV/只读 MySQL 数据源、导入批次和数据资产仍可照常使用；已有工作流也不会因为新增集合而被改写。需要使用旧数据资产时，继续按原页面操作。

## 前置条件与角色

需要登录平台，并拥有相应对象的权限：

- 查看集合和文件需要 `data_collection:read`、`data_file:read`；
- 创建集合需要 `data_collection:write`；
- 上传文件需要 `data_file:write`；
- 创建数据视图需要 `data_view:write`；
- 在工作流中编辑和运行输入节点，还需要相应的工作流编辑、发布和运行权限。

普通用户只能访问自己创建的集合、文件、版本和视图；数据管理员可按权限管理这些对象。没有权限的对象会按“不存在”处理。如果页面没有显示目标集合或文件，请先确认登录账号和授权范围。

## 创建数据集集合

1. 打开【数据中心】→【数据集管理】，点击【新建数据集】。
2. 填写名称；可选填写说明，例如区域、业务用途或资料范围。
3. 点击【创建】。新集合会出现在列表中，展开集合可查看文件成员、文件数量、容量和解析问题数量。

集合是资料的业务容器，不要求其中每个文件的列名一致。建议按业务范围建立集合，例如“东区 DMA 运行资料”，再把拓扑、流量、压力和点位资料分别作为文件加入。

## 上传异构文件

1. 在目标集合行点击展开，再点击【上传文件】。
2. 选择待上传的文件并等待上传完成。页面提示“文件已上传，正在解析”时，表示原件已接收，结构化解析仍在后台进行。
3. 返回集合的文件列表查看文件状态。一个逻辑文件在平台中可以保留多个不可变版本，文件列表当前展示可用的当前版本。

文件上传后可能进入以下状态：

| 页面状态 | 含义与处理 |
| --- | --- |
| 处理中 / 解析中 | 正在生成文件画像。等待状态变为可用后再打开预览。 |
| 可用 | 已有可读取的结构化版本，可以查看预览或创建数据视图。 |
| 格式不支持 | 原件仍被保留，但当前格式没有结构化预览；需要改用受支持的格式重新上传。 |
| 解析失败 | 原件已保留，但暂时不能创建结构化视图；检查文件内容后重新上传，或稍后重试。 |

集合列表中的“解析问题”是该集合内文件问题数量的汇总，不是单个文件的独立状态。

解析任务尚未完成时，不要把文件当作工作流输入。页面会定期刷新已展开集合的文件状态。

## 查看有限预览

在文件行点击【查看预览】，页面会打开该文件当前版本的预览。预览只显示有界样本，最多 50 行，并标出列名和推断类型；它不是完整文件的替代品。若尚无可预览版本，先等待解析完成。

预览读取失败时，先刷新页面并确认版本状态仍为可用。若仍无法读取，稍后重试；不要根据不完整预览推断完整文件内容。

## 创建不可变 DataView

DataView（数据视图）是从某个文件版本得到的固定列映射。创建后不能原地修改；要改变选择，应基于同一个文件版本创建新的视图。一个文件版本可以有多个互不影响的视图。

在预览面板中选择输出模式和列，然后点击【应用列映射】。应用成功后，平台会为当前文件版本创建并保存一个不可变 DataView：

### 表格输出

选择【表格输出】，点击列名以选中或取消列，至少保留一列，再点击【应用列映射】。表格视图适合把设备清单、拓扑资料或其他记录作为 `table` 输入。

### 时序输出

选择【时序输出】，依次选择角色后点击表头：

- 【时间列】必选；
- 【数值列】必选；
- 【点位列（可选）】用于标识测点，可不选。

时间列或数值列缺失时，不能应用映射。列无法满足时序要求时，创建视图会失败；请回到预览选择正确的列。DataView 只负责读取和投影，不会自动清洗、对齐、重采样、插值、换算单位或跨文件拼接。需要这些处理时，应在工作流中显式加入相应算子。

## 在工作流中使用 `data_file_input_v1`

1. 打开工作流编辑器，添加或选择 `data_file_input_v1` 节点。
2. 在节点属性面板的【数据文件输入】区域，依次选择【数据集合】、【数据文件】和【文件版本】。
3. 在当前文件版本的预览面板选择输出模式和列，并应用列映射以创建 DataView。完成后确认节点显示的文件、版本、输出模式和列摘要。
4. 根据视图类型将固定端口连接到后续节点：`table` 用于表格输出，`series` 用于时序输出。
5. 保存、校验并按工作流权限发布或运行。

节点始终只有 `table` 和 `series` 两个固定输出端口，不会随文件列数变化。需要多个时序输出时，为每个明确的 DataView 建立独立的输入绑定，不要期待运行时增加端口。

提交运行时，系统会冻结所选文件版本、DataView 映射和来源内容。之后上传的新版本或新建的其他视图不会改变已经提交的运行。若版本未准备好、视图列已不存在、权限不足或时序列无效，校验或运行时会给出明确错误，不会静默改用其他版本。

## 版本与成员关系

可以按以下关系理解页面中的对象：

```text
数据集集合 → 逻辑文件 → 文件版本 → 数据视图 → 工作流输入
```

集合只组织成员关系；逻辑文件代表一个资料项；文件版本代表一次上传或生成的固定内容；数据视图代表针对某个版本的固定列选择和语义。移除文件成员只会解除该集合中的关系，不等于删除底层文件。新的上传、列映射或工作流运行都不会改写已有版本和视图。

## 回收与常见失败处理

当通过相应管理接口发起删除时，集合或文件会进入平台既有的回收流程。清理集合首先移除成员关系；文件仍被活动集合或历史工作流运行引用时，系统会等待依赖解除后再继续清理。数据管理页面当前不提供这些对象的删除或回收操作；恢复或永久清理由数据管理员按平台回收流程处理。

常见错误可按下表处理：

| 提示或错误码 | 建议处理 |
| --- | --- |
| `DATA_COLLECTION_NOT_FOUND`、`DATA_FILE_NOT_FOUND`、`DATA_FILE_VERSION_NOT_FOUND`、`DATA_VIEW_NOT_FOUND` | 刷新列表，确认对象仍存在且账号有权限；不要反复猜测其他对象编号。 |
| `DATA_FILE_VERSION_NOT_READY` | 等待解析完成后刷新；不要绑定处理中版本。 |
| `DATA_FILE_PROFILE_FAILED` | 原件已保留，检查文件格式和内容后重新上传或重试。 |
| `DATA_FILE_FORMAT_UNSUPPORTED` | 当前格式不能生成结构化预览；原件仍被保留，需改用受支持格式重新上传。 |
| `DATA_VIEW_COLUMN_NOT_FOUND` | 重新打开该文件版本，选择仍存在的列并创建新视图。 |
| `DATA_VIEW_TIME_INVALID`、`DATA_VIEW_VALUE_INVALID` | 检查时间列和值列的内容和类型，修正源文件后重新上传。 |
| `DATA_FILE_STORAGE_UNAVAILABLE` | 文件内容暂时不可读，稍后安全重试。 |
| `WORKFLOW_INPUT_MISSING`、`WORKFLOW_PORT_INCOMPATIBLE`、`WORKFLOW_PORT_INACTIVE` | 检查节点是否已绑定文件版本和视图，并确认 `table`/`series` 端口与后续节点契约匹配。 |

如果上传失败但列表中没有新文件，先确认网络和文件本身，再重新选择文件。若同一问题持续出现，保留页面提示和任务标识，交由平台管理员排查。
