---
id: user.workflow-library
title: 工作流列表、起始结构与生命周期操作
document_type: user_guide
document_version: 1.1.0
status: published
locale: zh-CN
audience: [platform_user, developer, operator]
related_modules: [M05]
related_operators: []
related_apis: ["/api/v1/workflows", "/api/v1/workflow-templates", "/api/v1/workflows/from-template"]
owners: [product-team]
reviewed_at: 2026-08-21
summary: 说明工作流页面的卡片列表、起始结构向导，以及编辑、派生、克隆和回收站操作边界。
---

# 工作流列表、起始结构与生命周期操作

## 用途

【工作流】页面用于管理草稿、已发布版本和历史运行。发布版本不可变；需要修改已发布流程时，先形成草稿，再在编辑器中继续修改。

## 前置条件与角色

查看工作流需要工作流读取权限；新建、编辑草稿、派生或克隆需要工作流编辑权限；发布需要工作流发布权限。工作流回收站的查看和恢复受回收站权限和数据管理员边界控制。

## 操作步骤

1. 打开左侧导航【工作流】。页面以卡片视图显示流程名称、编码、草稿或发布状态、修订号、发布版本数量、运行次数和更新时间。
2. 使用【我的草稿】、【已发布流程】、【全部可见】或【回收站】筛选列表，在搜索框输入流程名称并点击【刷新】。回收站内容由有权限的管理员查看；无权限时列表可能为空。
3. 点击【新建工作流】进入起始结构向导。填写流程名称和可选说明，向导默认提供【空白工作流】，也可以选择当前列出的内置结构；点击【创建草稿并进入编辑器】后再拖拽节点、绑定数据和发布。
4. 在工作流卡片上按权限使用页面直接显示的操作：所有者可【编辑草稿】；有已发布版本时可【从最新版本派生】；符合可见性条件时可【克隆】；所有者或数据管理员可【移入回收站】。回收站中的流程由具备权限的人员使用【恢复】。
5. 需要查看版本时，在页面提供的版本区域选择版本并查看版本列表；从历史版本派生草稿前确认当前草稿修订没有被其他页面修改。

## 结果与失败处理

创建成功后进入工作流编辑器并得到一个草稿。发布会生成不可变版本；编辑已发布流程应回到草稿，不会覆盖历史版本。起始结构加载失败、没有可用结构、派生冲突或克隆失败时，保留页面提示并检查工作流权限、已发布版本和当前草稿修订。移入回收站后流程不再出现在普通列表；恢复或后续清理受权限以及版本/运行记录约束。
