---
id: user.operator-center
title: 算子中心、筛选、契约与默认参数
document_type: user_guide
document_version: 1.1.0
status: published
locale: zh-CN
audience: [platform_user, developer, admin]
related_modules: [M04]
related_operators: []
related_apis: ["/api/v1/operators", "/api/v1/operator-facets", "/api/v1/operators/{operator_code}/versions/{version}/default-parameters"]
owners: [product-team]
reviewed_at: 2026-08-21
summary: 说明如何在算子中心搜索和筛选算子，查看六类详情，并在权限允许时调整默认参数。
---

# 算子中心、筛选、契约与默认参数

## 用途

算子中心用于查找可组合的分析算子，查看其当前可用版本、输入输出契约、参数和运行状态。算子实际运行统一从工作流开始。

## 前置条件与角色

查看算子需要算子读取权限。调整算子状态或版本默认参数需要 `operator:manage` 权限；没有该权限的用户只能查看页面提供的信息。算子不可用时，应先处理页面显示的不可用原因。

## 操作步骤

1. 打开【算子中心】。在搜索框按名称或编码搜索，并使用【算子分类】和【成熟度】筛选。分类包括数据源、数据转换、算法、控制、输出和复合算子；成熟度包括生产、候选、实验和已弃用。
2. 需要更细筛选时打开【详细筛选】，按页面已提供的业务领域、任务类型、学习范式、训练要求、模型策略和运行环境筛选。具备管理权限时还可以按算子状态筛选已启用或已停用的算子。
3. 选择算子后，详情区域显示可用状态、当前版本、执行器类型、运行环境和成熟度。详情提供六个选项卡：【简介】、【契约与参数】、【训练与模型】、【版本与评估】、【文档】和【使用情况】。
4. 在【契约与参数】查看输入端口、输出端口、参数说明、约束和默认值；需要核对完整契约时展开页面提供的原始 JSON 契约。不要根据未显示的端口或字段推断算子能力。
5. 具备 `operator:manage` 权限时，在默认参数区域点击【调整默认参数】，编辑当前版本的默认参数并点击【保存生效】。保存后的参数作为后续新建工作流节点的默认执行配置；页面校验失败时先修正参数再保存。
6. 在【版本与评估】查看已登记版本的状态、成熟度和可用性，以及活动发布版本。模型训练或模型资产操作只使用【训练与模型】页当前显示的入口和权限。

## 结果与失败处理

筛选无结果时放宽搜索词或筛选条件。算子显示【不可运行】时，按详情中的原因检查版本、运行环境或管理员状态。无 `operator:manage` 权限时不会显示默认参数编辑入口；保存失败时保留页面错误信息并联系算子维护人员。页面没有把未展示的参数类型、端口或管理操作视为算子能力。
