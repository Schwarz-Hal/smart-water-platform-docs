# 文档写作规范

## 目标读者与结构

先说明用途，再说明操作或原理。面向使用者的文档使用业务语言；需要解释技术约束时，明确它对操作的影响。操作类文档应包含前置条件、所需角色、步骤、预期结果和失败处理。

## 表述

- 不使用“显然”“简单”“只需”“轻松”等措辞。
- 不写没有证据支持的性能、准确率或业务效果结论。
- 公开基准、论文结论与本项目测试结论必须明确区分。
- 时间、版本、参数和单位必须写清楚；算法编码、API 字段和错误码保留英文。
- 同一概念使用 `catalog/terminology.yml` 中登记的中文名称。

## 图表、公式与引用

- 图表服务于理解，必须有标题、来源和替代文字。
- 论文架构图优先重绘；直接引用时记录来源、许可证或使用依据。
- 公式首次出现时解释符号与单位；代码块只展示必要片段。

## 安全与隐私

截图和示例不得暴露真实账号、密钥、内部地址、生产业务数据或模型权重。文档不把 GitHub Pages 当作保密边界。
